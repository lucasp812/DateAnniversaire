<form method="post" action="{{ path('app_date_anniversaire_delete', {'id': date_anniversaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ date_anniversaire.id) }}">
    <button class="btn">Supprimer</button>
</form>
